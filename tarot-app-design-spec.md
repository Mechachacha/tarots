# タロット占いWebアプリ 実装設計書

> この設計書はCLIコーディングエージェント（Claude Code等）による実装を前提に書かれている。
> 元実装は Misskey Play (AiScript) 製のタロット占いプラグインであり、本ドキュメントはその仕様を
> 独立したReact Webアプリへ移植するための仕様書である。元のAiScriptコードのロジック（カード抽選、
> 結果整形、プロンプト生成）は仕様として参照し、UIの画面遷移構造は撤廃する。

---

## 1. 目的

ユーザーが質問とスプレッド（カードの引き方）を入力し、**ワンクリックで結果を即時表示する**
タロット占いWebサービスを構築する。誰でも利用できる公開サービスとして、Cloudflare Pages等の
静的ホスティングにデプロイする。

## 2. 元実装からの最重要変更点

元のAiScript版は `render_init` → `render_themes` → `draw_cards` という3段階の画面遷移を持つが、
**本実装ではこれを撤廃**し、以下の単一画面構成にする。

- 質問・スプレッド設定・テーマ入力は**すべて同一画面上**に表示する（折りたたみやモーダルでの分割は可、
  ページ遷移・ルーティングは不可）
- 「占う」ボタンを1回押すと、**ページ遷移せずに同画面内に結果セクションが表示される**
  （スクロールで結果へ自動遷移して良い）
- 「ひきなおし」「もういちど」は同画面内での状態リセットとして実装する（後述 6.6）
- ルーティングライブラリ（React Router等）は不要。単一ページ・単一コンポーネントツリーの
  状態管理（`useState`/`useReducer`）で完結させる

## 3. 技術スタック

| 項目 | 選定 |
|---|---|
| フレームワーク | React + Vite |
| 言語 | TypeScript |
| スタイリング | CSS Modules または Tailwind CSS（実装エージェントの判断に委ねる） |
| 状態管理 | React標準（`useState`/`useReducer`）。グローバルストアは不要な規模 |
| 永続化 | `localStorage`（アカウント機能・サーバー保存は対象外） |
| デプロイ先 | Cloudflare Pages（静的ビルド出力） |
| パッケージマネージャ | npm |

## 4. 画面構成（単一画面・ワイヤーフレーム）

```
┌─────────────────────────────────────────┐
│  タイトル                                  │
├─────────────────────────────────────────┤
│  [質問入力欄]                              │
│  [小アルカナ使用 トグル]                     │
│                                           │
│  スプレッド選択:                            │
│   ( ) プリセット: [ワンオラクル][スリーカード] │
│                  [ヘキサグラム][ケルト十字]   │
│   ( ) カスタム: [枚数入力] → [テーマ入力欄×N] │
│       （枚数を変えると入力欄が即座に増減）      │
│                                           │
│         [ 占う ]  ← ワンクリックで下に結果表示  │
├─────────────────────────────────────────┤
│  ▼ 結果セクション（占う実行後にここに出現）      │
│   質問：〇〇                                │
│   [カード1: 画像＋名前＋正逆位置＋テーマ]       │
│   [カード2: ...] ...（スプレッド形状に応じて配置）│
│                                           │
│   ▸ AIに聞くプロンプト（折りたたみ、コピー可）   │
│                                           │
│   [ひきなおし] [もういちど] [シェア]           │
├─────────────────────────────────────────┤
│  ▸ 過去の結果（折りたたみ、最大5件）           │
└─────────────────────────────────────────┘
```

スプレッド形状（ヘキサグラム・ケルト十字）は、カードを単純な縦並びリストではなく、
それぞれの伝統的な配置形状に近いCSS Grid / 絶対配置で表示する（4章の見た目改善要件）。

## 5. データモデル

### 5.1 カードマスタ（静的データ、`src/data/cards.ts`）

```ts
type Arcana = "major" | "minor";
type Suit = "wand" | "cup" | "sword" | "pentacle" | null;

interface Card {
  id: string;          // 例: "major_00", "minor_wand_ace"
  name: string;        // 日本語名 例: "愚者", "ワンドのエース"
  arcana: Arcana;
  suit: Suit;
  number: number;      // デッキ内の番号（0始まり）
  image: string;        // /cards/xxx.svg へのパス
}
```

大アルカナ22枚＋小アルカナ56枚（4スート×14ランク）＝78枚を元コードの命名規則
（`{suit}の{rank}`）に対応する形で定義する。

### 5.2 1回の占い結果

```ts
interface DrawResultItem {
  theme: string;
  cardId: string;
  position: "upright" | "reversed";
}

interface ReadingResult {
  question: string;
  useMinorArcana: boolean;
  items: DrawResultItem[];
  timestamp: number; // Date.now()
}
```

### 5.3 履歴（localStorage キー: `tarot_results_history`）

```ts
type HistoryStore = ReadingResult[]; // 先頭が最新、最大5件（元コードのMk:save仕様を継承）
```

## 6. 機能仕様

### 6.1 入力フォーム

- 質問テキスト入力（任意入力、空でも占い可。元コードも必須チェックなし）
- 小アルカナ使用トグル（true/false）
- スプレッド選択は以下の2モード:
  - **プリセットモード**: ボタンで選択すると `themeCount` と `themes[]` を自動セットする
    - ワンオラクル: 1枚, `["ワンオラクル"]`
    - スリーカード: 3枚, `["過去","現在","未来"]`
    - ヘキサグラム: 7枚, `["過去","現在","未来","環境","潜在意識","対応方法","結論"]`
    - ケルト十字: 10枚, `["現在の状況","障害と対策","顕在している事柄・意識","潜在している事柄・意識","近い過去","近い未来","質問者の立場や状況","質問者以外の人","質問者の願望","最終結果"]`
  - **カスタムモード**: 枚数（自然数のみ、0以下や小数は無効として弾く）を入力すると、
    その数だけテーマ入力欄を動的生成する。デフォルト値は `"{i+1}枚目"`。
    各欄は自由にラベルを編集できる（元コードの `card_themes[i]` 相当）。
- 「占う」ボタン押下で6.2のロジックを実行し、即座に結果セクションへ反映する
  （ページ遷移・モーダル遷移は発生させない）

### 6.2 抽選ロジック（元コード `draw_cards()` の仕様を移植）

```
function drawCards(themes: string[], useMinorArcana: boolean): DrawResultItem[] {
  const deck = useMinorArcana ? FULL_DECK_78 : MAJOR_ARCANA_22;
  const shuffled = shuffle(deck); // Fisher-Yatesで実装してよい（元コードはsort+乱数キー方式）
  return themes.map(theme => {
    const card = shuffled.shift();
    const position = Math.random() < 0.5 ? "upright" : "reversed";
    return { theme, cardId: card.id, position };
  });
}
```

- 重複なし抽選（元コードと同様、引いたカードはデッキから取り除く）
- 正位置/逆位置は各カードごとに独立して50%抽選
- テーマ数がデッキ枚数（22 or 78）を超える場合はエラー表示する（元コードには無いガードだが、
  実用上必要なため追加する）

### 6.3 結果表示

- 各カードを画像＋カード名＋正位置/逆位置＋対応テーマで表示
- スプレッドの種類に応じて配置を変える（5章参照）。プリセット外（カスタム）の場合は
  カード数に応じたグリッド表示で良い
- 表示時にカードを裏向き→表向きにめくるアニメーションを入れる（UI/UX改善要件）

### 6.4 AIに聞くプロンプト（元コードの折りたたみ機能を維持。実際のAI API連携は行わない）

元コードの生成テキストと同等のフォーマットでコピー用テキストを生成する:

```
質問：{question}
回答：
1. {theme1}…{cardName1} ({position1})
2. {theme2}…{cardName2} ({position2})
...
この結果を解釈して
```

折りたたみセクション内にこのテキストを表示し、「コピー」ボタンでクリップボードにコピーする
（`navigator.clipboard.writeText`）。

### 6.5 履歴

- 占う実行ごとに `ReadingResult` を `localStorage` の先頭に追加し、5件を超えたら末尾を削除
  （元コードの `resultsCollection` 仕様を継承）
- 履歴セクションは折りたたみリストで「n回前」として表示し、開くと質問文と結果を再表示する

### 6.6 ひきなおし／もういちど（画面遷移なしの状態リセット）

- **ひきなおし**: 直前と同じ `question` / `useMinorArcana` / `themes` を使い、6.2の抽選を再実行し
  結果セクションだけを更新する（入力フォームの値は保持）
- **もういちど**: 結果セクションを非表示にし、入力フォームを初期状態（質問空欄、小アルカナOFF、
  スプレッド未選択）に戻す。画面遷移は発生しない（同一ページ内の表示切り替えのみ）

### 6.7 シェア

元コードの `postFormButton`（Misskeyへの投稿）は標準Webでは使えないため、以下に置き換える:

- 結果テキスト＋ページURLをクリップボードにコピーするボタン
- 任意でX(Twitter)のシェアインテントURL（`https://twitter.com/intent/tweet?text=...`）を開くボタン

## 7. コンポーネント構成案

```
src/
  App.tsx                 // 状態管理のルート。フォーム状態・結果状態を保持
  components/
    QuestionInput.tsx
    MinorArcanaToggle.tsx
    SpreadSelector.tsx     // プリセット／カスタム切り替え＋テーマ入力欄の動的生成
    ThemeInputList.tsx
    DrawButton.tsx
    ResultSection.tsx      // スプレッド形状に応じたカード配置のレイアウト分岐
    CardItem.tsx           // 1枚のカード（画像・名前・正逆位置・めくるアニメーション）
    AiPromptBox.tsx        // 折りたたみ＋コピー
    HistoryPanel.tsx
    ShareButtons.tsx
  data/
    cards.ts               // 78枚のカードマスタ
    spreadPresets.ts        // プリセット定義
  lib/
    draw.ts                 // shuffle, drawCards
    history.ts               // localStorage読み書き
    promptText.ts             // AIプロンプト整形
  styles/
```

## 8. カード画像データソース

Rider-Waite-Smith版タロット（1909年発行、米英にて著作権満了・パブリックドメイン）を使用する。
以下のいずれかから画像を取得し、`public/cards/` 配下に配置する:

- https://github.com/mixvlad/TarotCards （78枚PNG＋メタデータが整理済み、パブリックドメイン）
- https://commons.wikimedia.org/wiki/Category:Rider-Waite_tarot_deck （Wikimedia Commons原本）

実装エージェントへの指示: 商用有償アセット（Etsy等）は使用しないこと。出典が明記された
パブリックドメイン／CC0データセットのみを使用し、`README` 等にクレジット表記を残すこと。

## 9. 非機能要件

- レスポンシブ対応（スマートフォン幅を基準に設計し、PC幅にも対応）
- ダークテーマを基調とする（タロットの雰囲気に合わせる）
- アクセシビリティ: フォーム要素にlabelを付与、ボタンにaria-labelを付与
- ビルド: `npm run build` で静的出力し、Cloudflare Pagesにそのままデプロイ可能な構成にする
- 外部APIキー・サーバーサイド処理は不要（完全クライアントサイド）

## 10. スコープ外（明示的に対象外）

- ユーザーアカウント機能・サーバーサイドでの履歴保存
- 実際のAI解釈API連携（プロンプトのコピー表示のみで完結させる）
- Misskey/AiScript固有API（`Mk:save`, `Mk:url`, `postFormButton`等）への依存
- 多言語対応（日本語のみで良い）

## 11. 受け入れ基準（チェックリスト）

- [ ] 質問・小アルカナ・スプレッド設定が同一画面上に表示され、画面遷移が発生しない
- [ ] プリセット選択でテーマ入力欄が自動生成・編集可能である
- [ ] カスタムモードで枚数変更時に入力欄が即座に増減する（自然数以外は弾く）
- [ ] 「占う」ボタン押下で結果が同画面内に表示される（ページ遷移なし）
- [ ] 抽選結果に重複カードが出ない／正逆位置がカードごとに独立して決まる
- [ ] AIプロンプトの折りたたみ・コピーが機能する
- [ ] localStorageに最大5件の履歴が保存・表示される
- [ ] ひきなおし／もういちどが画面遷移せずに動作する
- [ ] `npm run build` の出力がCloudflare Pagesにデプロイ可能である
